# The fixtures are compared against each other by CI on all three platforms: a quote on the page
# has to be a substring of a line in the history, so both sides need the same line endings.
tests/fixtures/*.jsonl text eol=lf
tests/fixtures/*.html text eol=lf
